
const locale=document.documentElement.lang.startsWith('pt')?'pt-BR':'en-US';const root=document.body.dataset.root||'..';const fmt=new Intl.NumberFormat(locale);const money=new Intl.NumberFormat(locale,{style:'currency',currency:'USD',maximumFractionDigits:0});function esc(v=''){return String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}function teamCode(n=''){return n.replace(/[^A-Za-z0-9]/g,'').slice(0,3).toUpperCase()||'CS2'}async function loadData(){try{const r=await fetch(`${root}/data/site.json?ts=${Date.now()}`);if(!r.ok)throw new Error();return await r.json()}catch(e){return null}}function renderRankings(d){const b=document.querySelector('#rankings-body');if(!b||!d)return;b.innerHTML=d.rankings.slice(0,10).map(t=>`<tr><td class="rank">${t.rank}</td><td><span class="teamid"><span class="teamlogo">${teamCode(t.team)}</span><b>${esc(t.team)}</b></span></td><td>${fmt.format(t.points)}</td><td class="source">${esc(t.region||'Global')}</td></tr>`).join('')}function renderEvents(d){const b=document.querySelector('#events');if(!b||!d)return;const k=locale==='pt-BR'?'pt':'en';b.innerHTML=d.events.map(e=>`<div class="event"><div><div><span class="badge ${e.status==='live'?'live':'up'}">${e.status==='live'?(k==='pt'?'AO VIVO':'LIVE'):(k==='pt'?'EM BREVE':'UPCOMING')}</span></div><h4>${esc(e.name)}</h4><p>${esc(e.dates[k]||e.dates.en)} · ${esc(e.location||'Online')}</p></div><div class="money">${e.prize?money.format(e.prize):'—'}</div></div>`).join('')}function renderNews(d){const b=document.querySelector('#newsgrid');if(!b||!d)return;const k=locale==='pt-BR'?'pt':'en';b.innerHTML=d.news.slice(0,3).map((n,i)=>`<article class="card news ${i===0?'large':'small'}"><span class="tag">${esc(n.category[k])}</span><h3>${esc(n.title[k])}</h3><p>${esc(n.summary[k])}</p><div class="meta"><span>${esc(n.date)}</span><span>${esc(n.source)}</span></div></article>`).join('')}function renderTicker(d){const b=document.querySelector('#tickeritems');if(!b||!d)return;const k=locale==='pt-BR'?'pt':'en';b.innerHTML=d.ticker.map(t=>`<span><b>${esc(t.label[k])}</b> ${esc(t.value[k])}</span>`).join('')}function renderMatches(d){const b=document.querySelector('#matches');if(!b||!d)return;const k=locale==='pt-BR'?'pt':'en';if(!d.matches?.length){b.innerHTML=`<div class="empty">${k==='pt'?'Partidas automáticas aguardando a chave da PandaScore.':'Automatic fixtures are waiting for the PandaScore key.'}</div>`;return}b.innerHTML=d.matches.slice(0,5).map(m=>`<div class="match"><div class="matchtop"><span>${esc(m.league||'CS2')}</span><span class="status">${m.status==='running'?(k==='pt'?'AO VIVO':'LIVE'):esc(m.when||'')}</span></div><div class="teams"><div><div class="teamrow"><span class="teamid"><span class="teamlogo">${teamCode(m.team1)}</span>${esc(m.team1)}</span><span>${m.score1??'-'}</span></div><div class="teamrow"><span class="teamid"><span class="teamlogo">${teamCode(m.team2)}</span>${esc(m.team2)}</span><span>${m.score2??'-'}</span></div></div><div class="score">BO${m.bo||3}</div></div></div>`).join('')}function renderFreshness(d){const e=document.querySelector('#freshness');if(!e||!d)return;const dt=new Date(d.generated_at);e.textContent=(locale==='pt-BR'?'Dados sincronizados: ':'Data synced: ')+dt.toLocaleString(locale)}loadData().then(d=>{renderRankings(d);renderEvents(d);renderNews(d);renderTicker(d);renderMatches(d);renderFreshness(d)});
